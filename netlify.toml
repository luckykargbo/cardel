# SALONEAUTOLINK — NETLIFY CONFIGURATION
[build]
  publish = "."
  functions = "functions"

[functions]
  included_files = ["database/**", "middleware/**", "services/**", "server.js"]
  external_node_modules = ["@libsql/client"]

# Secret Admin Gate URL rewrite (Renders Admin Suite)
[[redirects]]
  from = "/admin/get/get_chenor*"
  to = "/admin.html"
  status = 200

# Block direct access to admin.html and /admin
[[redirects]]
  from = "/admin.html"
  to = "/"
  status = 302
  force = true

[[redirects]]
  from = "/admin"
  to = "/"
  status = 302
  force = true

# Express API serverless function routing
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
