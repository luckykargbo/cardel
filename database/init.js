/**
 * database/init.js — Legacy file, superseded by db.js
 * All initialization (schema creation + seeding) is now handled
 * automatically by initDatabase() in db.js on first server start.
 *
 * You do NOT need to run this file manually.
 * Simply run: npm start
 */
console.log('ℹ️  Init is handled automatically by db.js. Run `npm start` to start the server.');
