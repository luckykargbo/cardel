Fix favourites badge accuracy, deduplication, and ID type coercion in main.js

- Add getValidFavourites() to filter out stale/deleted car IDs and enforce number type deduplication
- Update toggleFav and updateFavBadge to calculate exact count of valid saved vehicles currently in database
