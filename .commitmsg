Fix search overlay toggle and add quickSearch implementation

- Added .show class toggling to search overlay so search input opens reliably on clicking search button
- Added real-time live input filtering as the user types in search box
- Added quickSearch(term) function so popular tag buttons filter inventory and scroll smoothly to results
