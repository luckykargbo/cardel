# Secret Admin Gate URL (Renders Admin Suite)
/admin/get/get_chenor*   /admin.html   200

# Block direct access to admin.html and /admin (force redirect to homepage)
/admin.html              /            302!
/admin                   /            302!

# API routing (for any Netlify/CDN fallback)
/api/*                   /.netlify/functions/api   200
